num_envs: 12
max_updates: 30000
steps_per_update: 15
learning_rate: 0.0001
seed: 42
env_type: sync

gamma: 0.99
entropy_coef: 0.01
grad_clip: 1.0

terminal_rewards:
  win: 2.0
  loss: -2.0
  draw: -0.5

lr_decay_interval: 100
lr_decay_factor: 0.5
min_lr: 0.0003
log_interval: 10
